const path = require('path');

const root = path.resolve(__dirname, './../../') + '/';

module.exports.root = root;

module.exports.components = root + 'components/';

module.exports.core = root + 'core/';